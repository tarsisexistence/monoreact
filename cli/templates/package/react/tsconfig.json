{
  "extends": "../../settings/tsconfig.workspace.json",
  "compilerOptions": {
    "baseUrl": "src"
  },
  "include": ["src", "../../**/*.d.ts"],
  "exclude": ["src/**/*.test.*", "src/**/*.spec.*"]
}
